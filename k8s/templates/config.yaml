token: "{{ k3s_token }}"

tls-san:
{% for san in k3s_tls_san %}
  - {{ san }}
{% endfor %}

disable:
{% for option in k3s_disable %}
  - {{ option }}
{% endfor %}

etcd-expose-metrics: {{ k3s_expose_etcd_metrics }}
