kind: pipeline
type: docker
name: docker playbook

steps:
- name: docker playbook
  image: henrywhitaker3/ansible-terraform
  when:
    branch:
      - main
  commands:
  - echo $HOMELAB_SSH_KEY > ~/.ssh/id_rsa
