stages:
  - lint

image: henrywhitaker3/ansible-terraform

lint:
  stage: lint
  parallel:
    matrix:
      - PLAYBOOK: [docker/main.yaml]
  script:
    - task drone-vault-pass VAULT_PASS="$VAULT_PASS"
    - task lint PLAYBOOK=$PLAYBOOK
