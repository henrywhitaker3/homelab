---
- name: Configure mariadb servers

  hosts: mariadb

  vars:
    mysql_exporter_db_user: exporter
    mysql_exporter_db_password: "{{ mariadb_users[0].password }}"

  roles:
    # - user
    # - common
    # - crog
    # - node_exporter
    # - mariadb
    - mysql_exporter
