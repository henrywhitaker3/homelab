apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nats
  namespace: argo
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: nats
    server: https://kubernetes.default.svc
  project: apps
  source:
    path: kubernetes/k3s/apps/databases/nats/cluster/chart
    repoURL: https://gitlab.com/henrywhitaker3/homelab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
