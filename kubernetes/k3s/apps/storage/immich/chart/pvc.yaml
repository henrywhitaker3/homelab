apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
spec:
  resources:
    requests:
      storage: 100Gi
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
