apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
spec:
  resources:
    requests:
      storage: immich
  volumeMode: Filesystem
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
