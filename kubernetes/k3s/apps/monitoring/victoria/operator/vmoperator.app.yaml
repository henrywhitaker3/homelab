apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vmoperator
  namespace: argo
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: victoria
    server: https://kubernetes.default.svc
  project: apps
  source:
    path: kubernetes/k3s/apps/monitoring/vmoperator/chart
    repoURL: https://gitlab.com/henrywhitaker3/homelab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
