apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tinyauth
  namespace: argo
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: identity
    server: https://kubernetes.default.svc
  project: apps
  source:
    path: kubernetes/k3s/apps/misc/tinyauth/chart
    repoURL: https://gitlab.com/henrywhitaker3/homelab.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
