apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
data:
  # kubernetes.yaml: |
  #   mode: cluster
  kubernetes.yaml: ""
  settings.yaml: ""
  bookmarks.yaml: ""
  services.yaml: ""
  widgets.yaml: |
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
          showLabel: true
          label: k3s
