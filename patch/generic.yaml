---
- name: Patch generic ubuntu server

  hosts:
    - jump
    - speakers

  serial: 1

  tasks:
    - name: Apt upgrade
      become: true
      ansible.builtin.apt:
        update_cache: true
        upgrade: true

    - name: Reboot server
      become: true
      ansible.builtin.reboot:
