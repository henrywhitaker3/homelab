apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n
spec:
  template:
    spec:
      containers:
        - name: n8n
          env:
            - name: DB_MYSQL_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: db-password
                  name: n8n-secrets
