generic:
  nameOverride: theme-park
  fullnameOverride: theme-park

  replicaCount: 2

  image:
    repository: ghcr.io/onedr0p/theme-park
    pullPolicy: Always
    tag: "rolling"

  service:
    port: 8080

  ingress:
    enabled: false

  probes:
    type: tcpSocket
    options:
      port: http
