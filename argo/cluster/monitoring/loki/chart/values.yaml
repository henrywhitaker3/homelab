loki:
  loki:
    commonConfig:
      replication_factor: 1
    storage:
      type: 'filesystem'
  singleBinary:
    replicas: 1
    persistence:
      storageClass: longhorn
      size: 5Gi

  monitoring:
    selfMonitoring:
      enabled: false
      grafanaAgent:
        installOperator: false
