apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: crunchy
spec:
  endpoints:
  - port: exporter
  namespaceSelector:
    matchNames:
    - crunchy
  selector:
    matchLabels:
      postgres-operator.crunchydata.com/cluster: crunchy
