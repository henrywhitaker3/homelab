generic:
  nameOverride: theme-park
  fullnameOverride: theme-park

  replicaCount: 3

  image:
    repository: linuxserver/code-server
    pullPolicy: Always
    tag: "latest"

  service:
    port: 8443

  ingress:
    enabled: true
    annotations:
      kubernetes.io/tls-acme: "true"
      cert-manager.io/cluster-issuer: letsencrypt-prod
      nginx.ingress.kubernetes.io/auth-url: https://auth.plexmox.com/api/verify
      nginx.ingress.kubernetes.io/auth-signin: https://auth.plexmox.com
    hosts:
      - host: code.henrywhitaker.com
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
    - secretName: code-henrywhitaker-com-tls
      hosts:
        - code.henrywhitaker.com

  pvc:
    enabled: true
    size: 5Gi
    storageClassName: longhorn
    mountPoint: /config

  config:
    PUID: "1000"
    PGID: "1000"
    TZ:  Europe/London

  secrets:
    enabled: true
    key: vscode-server
